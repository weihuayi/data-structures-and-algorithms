# D005｜受限的线性结构：栈与队列

## 本质关系

栈与队列 = 给同一个线性表施加两种操作限制：栈限制为同一端插入、删除（LIFO），队列限制为一端进、另一端出（FIFO）。能力可以靠"限制"获得——操作越受限、行为越可预测、越能对顺序做保证；接口越窄、不变量越容易维护（top 恒指向最年轻元素；front 守头、rear 守尾），正确性越好保证。循环队列是"表示 + 工程权衡"的集中体现：取模把数组弯成环（同余的数学接口），判空判满撞车用"牺牲一个单元"或"加一个计数器"两种方案权衡。顺序栈/链栈、顺序队列/循环队列/链队，第三次回收"同一抽象、多种表示"。

## 接口方向

- **教材锚点**：《数据结构（C语言版）（第3版）》3.1–3.3、3.5——栈与队列的定义、顺序栈、链栈、循环队列；3.6 案例思想融入括号匹配推演。
- **生活连接**：餐盘（只从顶上放取）、电梯（先到后走）、撤销（最后做的先撤回）；排队与打印任务（先到先服务）。
- **工程连接**：AGV 任务队列（几千台车的任务按到达顺序派发）、消息缓冲削峰——实践种子点到。
- **数学接口**：栈 = 逆序器（最近未配对者先闭合）；取模 = 同余（"到头就绕回"，时钟是日常的同余）。
- **计算机接口**：函数调用栈预告（"谁在记住该回到哪里"），接 D006 栈与递归。
- **回收 D002**："先预测、再运行、再检查"的课堂口径。
- **回收 D003**：顺序表示（数组 + top 的顺序栈；顺序队列的假满）。
- **回收 D004**：链式表示（链栈栈顶位置判断、链队 front/rear 守两头）；"同一抽象、多种表示"。

## 主要教学目标

- 能说出栈与队列各自的操作限制，解释 LIFO/FIFO 语义如何由限制产生；
- 能用四个操作的栈接口完成括号匹配的手工推演（含"遍历完栈非空即错"）；
- 能解释假满的成因，写出循环队列的取模下标与两种判空判满方案，并完成 N=5 的手工推演；
- 能说出"接口越窄、不变量越容易维护"的理由，建立"限制换语义"的设计观。

## 主要问题

- 明明可以随便存取，为什么要主动受限？（生活案例的不自由是客观限制，数据结构的限制是主动选择的纪律）只允许在一端存取，得到什么？
- 为什么限制反而有用？栈的接口有多大？
- 顺序队列为什么会假满？怎样把数组弯成环？front == rear 是空还是满？
- 栈 vs 队列，限制各在哪、语义各是什么？

## 主要案例

撤销操作与括号匹配（栈，`({[}]` 手工推演）、排队与 AGV 任务队列（队列）、N=5 循环队列完整推演（假满 → 取模 → 判空判满撞车 → 两种方案）、餐盘/电梯/打印任务（生活类比组）。

## 维护关系

- `slides.py`：课件主维护源，共 25 页。
- `index.html`：由共享构建工具生成的课堂 HTML，不作为并列主源。
- **交互演示**：第 7、13、16 页嵌入共享层组件——`data-stack="brackets"`（括号匹配栈轨迹，含匹配成功与栈非空两种对照情形）、`data-queue="plain"`（顺序队列假满现场）、`data-queue="circ"`（N=5 循环队列完整推演：判满 → 出队 → rear 绕回 → 再次判满），步进式"先预测、再下一步、看依据"，组件与样式在 `tools/deck/`。
- `code/`：`stack_demo.c`（顺序栈 push/pop 轨迹 + `({[}]`/`({[]})` 括号匹配验证）与 `queue_demo.c`（Part 1 顺序队列假满复现；Part 2 牺牲单元方案的循环队列，front/rear 轨迹与判满条件生效）。
- **衔接**：以 D004 留问（"只允许在一头存取"）开场；留问"谁在记住该回到哪里"接 D006 栈与递归。
- **教师注记层**：沿用 `<details class="teacher-note">` 形式，共 8 处（D004 作业共性错误回顾——开场页；括号匹配提问脚本；AGV 任务队列工程细节；取模与同余的数学深化；循环队列指针口径对照——教材正文 vs 选择题 (3)；接口越窄与信息隐藏，辩证法已与 D004 留问页去重；出门条操作——落地学生侧观测回路；函数调用栈铺垫提示），投影默认收起。

## 教材接口

第 22 页用「教材定义」引用块锚定栈与队列的定义（教材 3.1 节），点明二者是"操作受限的线性表"；第 15 页标注"见教材 3.5 节"（循环队列的顺序存储与判空判满）；第 24 页给出教材导航（精读 3.1–3.3、3.5）与 v0.3 双层作业：A 书本习题为第 3 章选择题 (1)(3)(8)(12)(14)、算法设计题 (2)(7)（题号取自 `docs/design/exercise-candidates/ch3.md` 的 D005 候选，2026-09-21 经教师审定；选择题 (13) 因提取歧义存疑未布置），B 扩展作业为 `code/queue_demo.c` 判满条件补全（牺牲单元与计数器两方案，附预测与实测对照），另有自学（栈逆序输出、两栈实现队列思考）、挑战（三种括号匹配程序、算法设计题 (4) 后缀表达式求值）、探究（教材 3.6 案例轨迹）条目。

## 课堂时间预算

90 分钟连堂，克制版估算（单位：分钟）：

- 封面与回到问题与限制思想 6 / 栈定义与接口 8 / 括号推演 8 / 顺序栈与演示与链栈 12 / 队列定义与 FIFO 与假满推演 11 / 取模与判空判满与循环推演 14 / 演示与链队 9 / 对照与收口与课后与留问 12 ≈ 80，缓冲 10。
- **超时风险（按 D003/D004 实测规律）**：内容预算 80 + 注记展开约 10 ≈ 90，缓冲仅 10 分钟，注记若全部展开必然超时。砍页优先级：① 第 17 页 queue_demo 演示整页砍（第 13、16 页动画已覆盖假满与判满轨迹，代码演示转课后自行运行）；② 第 15 页判空判满压为一方案 + 一句"标志位方案见算法设计题 (7)"；③ 变体之外再压 AGV 注记展开。
- **建议断点**（两节 45 分钟之间）：第 10 → 11 页（栈 → 队列）。
- **可砍页**：见超时风险的砍页优先级；留问页可整页跳过（下一课开场自带回收）。
- 注记全展开约 +10 分钟，需计入预算；步进演示与课堂提问绑定（先预测、再下一步），时间计入所在页预算，不额外增加总时长。
